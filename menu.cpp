#include "menu.h"

#include <TFT_eSPI.h>

#include "display.h"
#include "header.h"
#include "buttons.h"
#include "settings.h"


// ============================================================
// MENU
// ============================================================

void menuDraw()
{
  // ----------------------------------------------------------
  // Fond
  // ----------------------------------------------------------

  tft.fillScreen(
    COLOR_BACKGROUND
  );


  // ----------------------------------------------------------
  // Header 1
  // ----------------------------------------------------------

  header1Draw();


  // ----------------------------------------------------------
  // Header 2
  // ----------------------------------------------------------

  header2Draw(
    "MENU"
  );


  // ----------------------------------------------------------
  // Bouton 1
  // ----------------------------------------------------------

  buttonDraw(
    BUTTON_X,
    CONTENT_Y + 10,
    BUTTON_WIDTH,
    BUTTON_HEIGHT,
    "PAGE 1"
  );


  // ----------------------------------------------------------
  // Bouton 2
  // ----------------------------------------------------------

  buttonDraw(
    BUTTON_X,
    CONTENT_Y
      + 10
      + BUTTON_HEIGHT
      + BUTTON_SPACING,
    BUTTON_WIDTH,
    BUTTON_HEIGHT,
    "PAGE 2"
  );


  // ----------------------------------------------------------
  // Bouton 3
  // ----------------------------------------------------------

  buttonDraw(
    BUTTON_X,
    CONTENT_Y
      + 10
      + 2 * (
        BUTTON_HEIGHT
        + BUTTON_SPACING
      ),
    BUTTON_WIDTH,
    BUTTON_HEIGHT,
    "PAGE 3"
  );
}